export const ConditionEngine = {
  // Placeholder for future logic e.g., {{#if rsvp_url}}...{{/if}}
  evaluate(content: string, _variables: Record<string, string>): string {
    // Currently just returns content directly. Built for future extensibility.
    return content;
  },
};
